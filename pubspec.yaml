name: squirrel
description: Create installable desktop apps
version: 0.2.0
author: Ani Betts <anais@anaisbetts.org>, RustamG
homepage: https://github.com/RustamG/squirrel-flutter

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=2.0.1"

dependencies:
  args: ^2.3.0
  flutter:
    sdk: flutter
  jinja: ^0.3.0
  package_config: ^2.0.2
  path: ^1.8.0
  yaml: ^3.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.11.0
